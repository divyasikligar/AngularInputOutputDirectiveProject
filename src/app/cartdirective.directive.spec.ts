import { CartdirectiveDirective } from './cartdirective.directive';

describe('CartdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CartdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
